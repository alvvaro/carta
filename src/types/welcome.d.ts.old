export interface RTVEWelcome {
  version: number;
  fechaModificacion: Date;
  expirationTime: number;
  refresh: number;
  dashon: boolean;
  digitalVOD: boolean;
  digitaLIVE: boolean;
  tv: boolean;
  vw: number;
  segment: number;
  segmentc: number;
  subsaux: boolean;
  apiAux: boolean;
  login: boolean;
  adstv: boolean;
  adsLive: boolean;
  urlLogo: string;
  bannerPromo: string;
  bannerJJOO: BannerJJOO;
  imagesShorts: ImagesShorts;
  bannerLogin: BannerLogin;
  stats: Stats;
  bannerPuntua: BannerPuntua;
  television: Television;
  radio: Radio;
}

interface BannerJJOO {
  fechaInicio: Date;
  fechaFin: Date;
}

interface BannerLogin {
  title: string;
  active: boolean;
  imagemobile: string;
  imagetablet: string;
  imageTV: string;
  closeAllowed: boolean;
}

interface BannerPuntua {
  title: string;
  active: boolean;
  imagemobile: string;
  imagetablet: string;
  text: string;
  textButton: string;
}

interface ImagesShorts {
  imagemobile: string;
  imagetablet: string;
}

interface Radio {
  portada: RadioPortada[];
  menuBloques: RadioMenuBloque[];
  secciones: RadioSecciones;
}

interface RadioMenuBloque {
  orden: number;
  title: string;
  menuItems: PurpleMenuItem[];
}

interface PurpleMenuItem {
  orden: number;
  title: string;
  resaltado: boolean;
  tipo: string;
  urlContent?: string;
  imgBackground?: string;
  decorado?: boolean;
  menuItems?: MenuItemMenuItem[];
  mostrarlogado?: string;
  so?: string;
}

interface MenuItemMenuItem {
  orden: number;
  title: string;
  resaltado: boolean;
  tipo: Tipo;
  urlContent?: string;
  imgBackground?: string;
  logo?: string;
  pais?: string;
  decorado?: boolean;
  appIOS?: string;
  schemeIOS?: string;
  appAndroid?: string;
  id?: string;
  subscriptor?: boolean;
}

enum Tipo {
  Descargas = 'descargas',
  IntentApp = 'intentApp',
  Mislistas = 'mislistas',
  Portada = 'portada',
  PortadaCanalTematico = 'portadaCanalTematico',
  Seguirviendo = 'seguirviendo',
}

interface RadioPortada {
  orden: number;
  title: string;
  urlContent: string;
  tipo: string;
  subTitle?: null;
  numLayout?: number;
}

interface RadioSecciones {
  directosRadio: DirectosRadio;
  portada: DirectosRadio;
  buscadorAZ: BuscadorAZ;
  directos: PurpleDirectos;
  parrilla: Parrilla;
  nowAndNext: NowAndNext;
  buscador: Buscador;
  territoriales: Territoriales;
}

interface Buscador {
  urlContent: string;
  tituloLomasBuscado: string;
  urlBuscadorPredef: string;
  urlLomasBuscado: string;
  generospeliculas?: Generospelicula[];
}

interface Generospelicula {
  'nav-pag_name': string;
  'nav-pag_url': string;
}

interface BuscadorAZ {
  canales: CategoriaElement[];
  categorias: CategoriaElement[];
}

interface CategoriaElement {
  orden: number;
  title: string;
  urlContent: string;
}

interface PurpleDirectos {
  urlContent: string;
  urlbasestartover: string;
}

interface DirectosRadio {
  urlContent: string;
}

interface NowAndNext {
  emisoras: Emisora[];
}

interface Emisora {
  tipo: string;
  urlContent: string;
}

interface Parrilla {
  agrupadores: Agrupadore[];
}

interface Agrupadore {
  orden: number;
  title: string;
  canales: AgrupadoreCanale[];
}

interface AgrupadoreCanale {
  orden: number;
  title: string;
  logo: string;
  urlContent: string;
  idAsset: string;
}

interface Territoriales {
  informativos: string;
  emisoras: string;
}

interface Stats {
  adobe: boolean;
  comscore: boolean;
  muxData: boolean;
  userTracker: boolean;
  conviva: boolean;
  kantar: boolean;
}

interface Television {
  portada: TelevisionPortada[];
  menuBloques: TelevisionMenuBloque[];
  secciones: TelevisionSecciones;
}

interface TelevisionMenuBloque {
  orden: number;
  title: string;
  menuItems: FluffyMenuItem[];
}

interface FluffyMenuItem {
  orden: number;
  title: string;
  resaltado: boolean;
  tipo: string;
  subscriptor?: boolean;
  imgBackground?: string;
  urlContent?: string;
  decorado?: boolean;
  menuItems?: MenuItemMenuItem[];
  mostrarlogado?: string;
  so?: string;
}

interface TelevisionPortada {
  orden: number;
  tipo: string;
  title: string;
  urlContent?: string;
  mostrarSoloEnEmision?: boolean;
}

interface TelevisionSecciones {
  subscriptor: boolean;
  portada: SeccionesPortada;
  auxiliares: Auxiliares;
  desconexionTerritorial: DesconexionTerritorial;
  canalestematicos: Canalestematico[];
  buscadorAZ: BuscadorAZ;
  directos: FluffyDirectos;
  parrilla: Parrilla;
  buscador: Buscador;
  showDateEmision: string[];
  adsEnabled: string[];
  especiales: Especiale[];
  concursos: Concurso[];
}

interface Auxiliares {
  mdandroid: string;
  hlios: string;
}

interface Canalestematico {
  nombre: string;
  id: string;
  permalink: string;
}

interface Concurso {
  id: string;
  activo: boolean;
  feed: string;
}

interface DesconexionTerritorial {
  idDirectoPrincipal: string;
  horaInicio: string;
  horaFin: string;
  urlDesconexiones: string;
  urlLocalizacion: string;
}

interface FluffyDirectos {
  urlContent: string;
  urlbasestartover: string;
  programsstartover: Programsstartover[];
}

interface Programsstartover {
  programId: string;
  idGolumi: string;
  sgce: string;
}

interface Especiale {
  programRefid: string;
  typeId: number;
}

interface SeccionesPortada {
  urlContent: string;
  addParams: boolean;
}
